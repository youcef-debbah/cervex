#!/bin/bash
# Captures the artifacts from a previous build into the next incremental build.

echo "running pre save hook..."
# hook script
echo "...hook done"

# Execute the default S2I script
exec /usr/libexec/s2i/save-artifacts
